params {
    ground_truth = "${projectDir}/assets/ground_truth.tsv"
    n_pos        = 50
    n_neg        = 50
    seed         = 42
    model        = 'claude-haiku-4-5-20251001'
    criterion    = 'Is there a transcription factor perturbation in this experiment?'
    fields       = 'organism_scientific_name,platform_gpl_accession'
    outdir       = 'results'
}

profiles {
    conda {
        conda.enabled = true
        process.conda   = "${projectDir}/environment.yml"
    }
}
